<?xml version="1.0" encoding="utf-8"?>
<!--
  Localised store descriptions. The client requested this name directly
  (GET /dime/storedesc-fre_fr.xml, User-Agent ProtoHttp 1.3/DS 8.18.3.0),
  the suffix following the French install. Nothing in the launcher's seven
  flags depends on descriptions, so this stays deliberately empty rather than
  inventing a schema: it exists so the fetch succeeds instead of 404-ing.
-->
<storedesc>
</storedesc>
